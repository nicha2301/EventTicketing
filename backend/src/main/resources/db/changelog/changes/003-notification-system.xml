<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="003-notification-system" author="eventticketing">
        <comment>Thêm bảng và trường cho notification system</comment>

        <!-- Bảng device_tokens để lưu trữ token thiết bị cho push notification -->
        <createTable tableName="device_tokens">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="uuid">
                <constraints nullable="false" foreignKeyName="fk_device_token_user" references="users(id)"/>
            </column>
            <column name="token" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="device_type" type="varchar(50)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="timestamp">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp">
                <constraints nullable="false"/>
            </column>
            <column name="is_active" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
        </createTable>
        
        <!-- Index cho device_tokens -->
        <createIndex tableName="device_tokens" indexName="idx_device_tokens_user_id">
            <column name="user_id"/>
        </createIndex>
        <createIndex tableName="device_tokens" indexName="idx_device_tokens_token">
            <column name="token"/>
        </createIndex>
        
        <!-- Bảng notifications để lưu trữ lịch sử thông báo -->
        <createTable tableName="notifications">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="uuid">
                <constraints nullable="false" foreignKeyName="fk_notification_user" references="users(id)"/>
            </column>
            <column name="title" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="content" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="notification_type" type="varchar(50)">
                <constraints nullable="false"/>
            </column>
            <column name="reference_id" type="uuid">
                <constraints nullable="true"/>
            </column>
            <column name="reference_type" type="varchar(50)">
                <constraints nullable="true"/>
            </column>
            <column name="created_at" type="timestamp">
                <constraints nullable="false"/>
            </column>
            <column name="is_read" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="read_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
        </createTable>
        
        <!-- Index cho notifications -->
        <createIndex tableName="notifications" indexName="idx_notifications_user_id">
            <column name="user_id"/>
        </createIndex>
        <createIndex tableName="notifications" indexName="idx_notifications_reference_id">
            <column name="reference_id"/>
        </createIndex>
        
        <!-- Thêm trường notification_preferences vào bảng users -->
        <addColumn tableName="users">
            <column name="notification_preferences" type="jsonb" defaultValue="{}">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>
</databaseChangeLog> 